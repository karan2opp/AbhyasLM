# From your Clerk dashboard (clerk.com)
VITE_CLERK_PUBLISHABLE_KEY=

# Leave empty in local dev — Vite's dev proxy forwards /api to the backend.
# Set this to your deployed API's URL for a production build (no trailing slash).
VITE_API_BASE_URL=
